# Configuração da conexão com o banco de dados Oracle
spring.datasource.url=jdbc:postgresql://localhost:5432/Biblioteca
spring.datasource.username=postgres
spring.datasource.password=masterkey
spring.datasource.driver-class-name=org.postgresql.Driver
server.port=8080

### Anotações para configuração ###

# Habilita logs dos comandos SQL no console
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Adiciona a mensagem de erro especificado na exception de Status Code
server.error.include-message=always

# Propriedade para criação da database com a JPA
#spring.jpa.hibernate.ddl-auto=create